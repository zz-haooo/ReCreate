# ReCreate Core Dependencies
# Install: pip install -r requirements.txt

# LLM providers
litellm>=1.75.0
openai>=1.0.0
anthropic>=0.30.0

# Core utilities
pyyaml
jinja2
requests
httpx
tenacity
rich
python-dotenv
typer
platformdirs

# UI components
textual
prompt_toolkit

# Data processing
datasets
numpy
pandas

# Docker SDK (for sandboxed evaluation)
docker

# Optional: for DS-1000 domain (install separately if needed)
# matplotlib
# scipy

